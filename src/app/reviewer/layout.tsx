import { PostTubeShell } from "@/features/posttube/components/PostTubeShell"

export default function ReviewerLayout({ children }: { children: React.ReactNode }) {
  return <PostTubeShell>{children}</PostTubeShell>
}
